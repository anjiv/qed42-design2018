{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region <div>.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 * - copyrightText: The custom block that is loaded in .theme file.
 * - social: The menu block that is loaded in .theme file.
 * - helpText: The custom block that is loaded in .theme file.
 * @see template_preprocess_region()
#}

{% include '@organisms/00-global/01-footer.twig'
   with {
   'copyrightText': copyrightText,
   'social': social,
   'helpText': helpText
  }
%}
